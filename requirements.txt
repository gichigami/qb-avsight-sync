# Runtime dependencies for the QuickBooks → AvSight sync Lambdas.
#
# Versions are pinned to what is actually running in production (read out of the
# deployed package on 2026-08-28) so a rebuild from this repo reproduces prod.
#
# NOT packaged — provided by Lambda layers, do not vendor these into the zip:
#   pandas, numpy, boto3, botocore  -> AWSSDKPandas-Python311 layer
#   pioneer_email                   -> pioneer-email layer (internal)
#
# See scripts/build_and_deploy.sh, which installs this file into the package.

requests==2.32.5
simple-salesforce==1.12.9

# Transitive deps, pinned to the versions present in the deployed package.
attrs==25.4.0
certifi==2026.2.25
cffi==2.0.0
charset-normalizer==3.4.5
cryptography==46.0.5
idna==3.11
isodate==0.7.2
lxml==6.0.2
more-itertools==10.8.0
platformdirs==4.9.4
pycparser==3.0
PyJWT==2.12.1
pytz==2026.1.post1
requests-file==3.0.1
requests-toolbelt==1.0.0
typing_extensions==4.15.0
urllib3==2.6.3
zeep==4.3.2

# Local development / testing only (not deployed):
#   pandas>=2.0.0
#   boto3>=1.28.0
#   psutil>=5.9.0   # system memory info; unavailable and unused on Lambda
